// ─────────────────────────────────────────────────────────────────
// test_setup_sanity.cpp
//
// 이 파일의 목적: "빌드 시스템 뼈대"가 끝까지 동작하는지 확인하는
// 최소한의 테스트. 아직 mapf_core에는 진짜 코드가 없으므로(소스가
// 0개인 빈 라이브러리), 알고리즘과 무관한 가장 단순한 사실 하나만
// 확인한다 — "GoogleTest가 정상적으로 연결되어 실행되는가."
//
// 다음 단계(Cell/Map/Agent 구현)부터는 이 파일을 지우고 진짜
// 테스트로 교체한다.
// ─────────────────────────────────────────────────────────────────
#include <gtest/gtest.h>

TEST(SetupSanity, BuildSystemWorks) {
    EXPECT_EQ(1 + 1, 2);
}
